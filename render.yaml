services:
  # Background worker - stays running without needing HTTP traffic
  - type: worker
    name: topo-exchange-bot
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    envVars:
      - key: BOT_TOKEN
        fromSecret: true